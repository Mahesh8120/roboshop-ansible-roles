- name: demon reload
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: "enable and start {{component}}"
  ansible.builtin.service: 
    name: "{{component}}"     
    state: started
    enabled: yes
