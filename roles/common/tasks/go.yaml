- name: install golang
  ansible.builtin.dnf:
    name: golang
    state: present

- name: install dependencies
  ansible.builtin.shell: go mod init dispatch ; go get ; go build 
  loop:
  args: 
    chidr: /app